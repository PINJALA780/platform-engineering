apiVersion: v1
kind: Service

metadata:
  name: ${{ values.name }}
  namespace: ${{ values.name }}
  labels:
    app: ${{ values.name }}

spec:
  type: ClusterIP

  selector:
    app: ${{ values.name }}

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
